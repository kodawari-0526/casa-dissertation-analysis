# Street View metadata

This stage resolves metadata and panorama IDs for the four requested headings. It writes the non-secret request parameters needed to reproduce the sampling design, but it neither downloads images nor writes API keys or signed request URLs.

The image collection used in the dissertation was made in May 2026. Street View imagery and caches are excluded from this repository; panorama IDs and derived audit rows are handled separately under the publication boundary.
