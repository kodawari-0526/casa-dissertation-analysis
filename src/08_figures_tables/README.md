# Figures and tables

This stage regenerates the study-area map, workflow, PSSI maps, equity screen, Local Moran diagnostics, component maps, correlations, model coefficient plot and all computed result tables. It also writes the audit-target, scoring-framework and prompt-structure method tables and collects the tables in one workbook.

The street-view validation montage is intentionally not produced by the public workflow because its source images cannot be redistributed. A local restricted copy can remain with the dissertation evidence, but it must not be added to this repository.
